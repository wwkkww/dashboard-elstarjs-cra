const appConfig = {
    apiPrefix: '/api',
    authenticatedEntryPath: '/app/main/dashboard',
    unAuthenticatedEntryPath: '/sign-in',
    tourPath: '/',
    locale: 'en',
    enableMock: true,
}

export default appConfig
